# Temporal harmony v2 frozen selection

**Decision: retain v1.**

Every v2 candidate failed the frozen fragmentation and regions-per-minute gates on both microphone and pickup captures. Accuracy and boundary gains cannot override the predeclared stability requirements.

p00 was not accessed. Production weights and hybrid defaults remain unchanged.
